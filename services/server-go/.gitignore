# Build artifacts
# `go build ./...` from this directory writes a binary named after the
# module's last path segment (server-go) into this directory.
/server-go

# Test binaries (`go test -c`) and test output captures.
*.test
*.out

# Coverage artifacts not under GOCACHE / GOPATH.
/coverage.out
/coverage.html
